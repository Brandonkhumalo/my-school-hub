version: "3.9"

# ──────────────────────────────────────────────────────────────
# My School Hub — Go + Django Microservices
# ──────────────────────────────────────────────────────────────
# Architecture:
#   Internet → Go Gateway (:8080) → Django (:8000)
#                                 → Go Workers (:8081)   — bulk CSV imports
#                                 → Go Services (:8082)  — PDF, PayNow, email, WhatsApp
#                                 → Go Admissions (:8091) — online application intake/workflow
#                                 → Django Admissions Ops (:8092) — form templates/compliance
#
# Start everything:   docker compose up --build
# Run in background:  docker compose up -d --build
# Stop:               docker compose down
# ──────────────────────────────────────────────────────────────

services:

  # ── Redis (Celery broker + cache) ──────────────────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Go API Gateway (~15MB RAM) ─────────────────────────────
  # Handles: JWT auth, token blacklist, audit logging, rate limiting
  # Proxies all requests to Django, routes bulk imports to Go Workers
  gateway:
    build:
      context: ./go-gateway
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - ./School_system/.env
    environment:
      GATEWAY_PORT: "8080"
      DJANGO_UPSTREAM: "http://web:8000"
      GO_WORKERS_UPSTREAM: "http://workers:8081"
      GO_SERVICES_UPSTREAM: "http://services:8082"
      GO_ADMISSIONS_UPSTREAM: "http://admissions-api:8091"
      DJANGO_ADMISSIONS_OPS_UPSTREAM: "http://admissions-ops:8092"
    depends_on:
      web:
        condition: service_started
      workers:
        condition: service_started
      services:
        condition: service_started
      admissions-api:
        condition: service_started
      admissions-ops:
        condition: service_started

  # ── Django API (Gunicorn) — now internal only ──────────────
  # No longer exposed on host port — gateway handles all external traffic
  web:
    build:
      context: ./School_system
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8000"
    env_file:
      - ./School_system/.env
    environment:
      CELERY_BROKER_URL: redis://redis:6379/0
      USE_GO_GATEWAY: "true"
      GO_SERVICES_URL: "http://services:8082"
    volumes:
      - ./School_system/db.sqlite3:/app/db.sqlite3
      - media_files:/app/media
    depends_on:
      redis:
        condition: service_healthy

  # ── Go Bulk Workers (~10MB RAM) ────────────────────────────
  # Handles: CSV bulk imports (students, results, fees)
  # Streams CSV line-by-line, batch INSERTs via pgx
  workers:
    build:
      context: ./go-workers
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8081"
    env_file:
      - ./School_system/.env
    environment:
      WORKERS_PORT: "8081"

  # ── Go Services (~12MB RAM) ───────────────────────────────
  # Handles: PDF report cards, PayNow payments, email (Resend), WhatsApp
  # Uses goroutines for non-blocking external API calls
  services:
    build:
      context: ./go-services
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8082"
    env_file:
      - ./School_system/.env
    environment:
      SERVICES_PORT: "8082"

  # ── Go Admissions API (~12MB RAM) ─────────────────────────
  # Handles: per-school online submissions, workflow statuses, reviewer assignment
  admissions-api:
    build:
      context: ./microservices/online-admission/go-admissions-api
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8091"
    environment:
      ADMISSIONS_PORT: "8091"

  # ── Django Admissions Ops ──────────────────────────────────
  # Handles: form templates, field metadata, compliance/retention policies
  admissions-ops:
    build:
      context: ./microservices/online-admission/django-admissions-ops
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8092"
    environment:
      DJANGO_DEBUG: "true"
      DJANGO_SECRET_KEY: "admissions-local-secret-key"

  # ── Celery Worker ───────────────────────────────────────────
  celery:
    build:
      context: ./School_system
      dockerfile: Dockerfile
    restart: unless-stopped
    command: >
      celery -A School_system worker
        --loglevel=info
        --concurrency=2
        --queues=celery
    env_file:
      - ./School_system/.env
    environment:
      CELERY_BROKER_URL: redis://redis:6379/0
      USE_GO_GATEWAY: "true"
      GO_SERVICES_URL: "http://services:8082"
    volumes:
      - ./School_system/db.sqlite3:/app/db.sqlite3
      - media_files:/app/media
    depends_on:
      redis:
        condition: service_healthy
      web:
        condition: service_started

  # ── Celery Beat (optional periodic tasks) ──────────────────
  celery-beat:
    build:
      context: ./School_system
      dockerfile: Dockerfile
    restart: unless-stopped
    command: >
      celery -A School_system beat
        --loglevel=info
        --scheduler django_celery_beat.schedulers:DatabaseScheduler
    env_file:
      - ./School_system/.env
    environment:
      CELERY_BROKER_URL: redis://redis:6379/0
      USE_GO_GATEWAY: "true"
    volumes:
      - ./School_system/db.sqlite3:/app/db.sqlite3
    depends_on:
      redis:
        condition: service_healthy
      web:
        condition: service_started

volumes:
  redis_data:
  media_files:
