# Core Django
Django==5.2.1
djangorestframework==3.16.0
django-cors-headers==4.3.1
django-filter==25.1
dj-database-url==3.0.1
whitenoise==6.9.0

# Auth
PyJWT==2.9.0

# Async task queue
celery==5.5.3
kombu==5.5.3
redis==6.2.0
django_celery_results==2.6.0
tblib==3.1.0

# Config / env
python-decouple==3.8

# HTTP & utilities
requests==2.31.0
Pillow==11.2.1
qrcode==8.2

# Production WSGI server
gunicorn==23.0.0

# Database (PostgreSQL for production; SQLite used in dev)
psycopg2-binary==2.9.7

# Caching (Redis backend for Django cache framework)
django-redis==5.4.0

# API Documentation
drf-spectacular==0.28.0

# PDF generation (report cards)
reportlab==4.2.2

# AI / Machine Learning (grade predictions)
scikit-learn==1.5.2
numpy==1.26.4

# PayNow Zimbabwe payments
paynow==1.0.8

# Rate limiting
django-ratelimit==4.1.0

# AWS S3 media storage (optional — only needed when AWS_STORAGE_BUCKET_NAME is set)
django-storages==1.14.4
boto3==1.35.36
